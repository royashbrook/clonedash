name: Deploy Clone Dash
on:
  workflow_dispatch:
  workflow_run:
    workflows: [Clone Dash checks]
    branches: [main]
    types: [completed]
permissions:
  contents: read
concurrency:
  group: clonedash-production
  cancel-in-progress: false
jobs:
  deploy:
    if: >-
      (github.event_name == 'workflow_dispatch' && github.ref == 'refs/heads/main') ||
      (github.event.workflow_run.conclusion == 'success' &&
       github.event.workflow_run.event == 'push' &&
       github.event.workflow_run.head_branch == 'main' &&
       github.event.workflow_run.head_repository.full_name == github.repository)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          # A late check for an old merge must never displace the newest release.
          ref: main
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run check
      - run: npm test
      - run: npm run test:mutations
      - run: RELEASE_BUILD=1 npm run build
      - run: npm run test:release
      - run: npx playwright install --with-deps chromium webkit
      - run: npm run test:browser
      - name: Refuse a superseded release
        run: |
          git fetch origin main
          test "$(git rev-parse HEAD)" = "$(git rev-parse origin/main)"
      - run: npx wrangler deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
