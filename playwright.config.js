import { defineConfig } from '@playwright/test';
export default defineConfig({ testDir: './tests', testMatch: '**/*.spec.js', timeout: 60000, workers: 1, use: { baseURL: process.env.GAME_URL || 'http://127.0.0.1:4190', viewport: { width: 932, height: 430 }, trace: 'retain-on-failure' }, projects: [{ name: 'chromium', use: { browserName: 'chromium' } }, { name: 'webkit', use: { browserName: 'webkit' } }], webServer: process.env.GAME_URL ? undefined : { command: 'npm run dev', url: 'http://127.0.0.1:4190', reuseExistingServer: !process.env.CI } });
